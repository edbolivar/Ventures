const customer = 'customer';

const agent = 'agent';

const admin = 'admin';

const superAdmin = 'super-admin';

const anonymous = 'anonymous';

module.exports = {
  customer,
  agent,
  admin,
  superAdmin,
  anonymous,
};
